<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Details </title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Helvetica, sans-serif;
        }

        .container {
            max-width: 1000px;
            margin: auto;
            padding: 10px;
            text-align: start;
            display: flexbox;
        }

        /* Créez une grille pour aligner les boîtes */
        .grid {
            display: flexbox;
            grid-template-columns: repeat(auto-fill, minmax(20px, 1fr));
            grid-gap: 10px;
        }

        /* Style des boîtes */
        .box {
          
            padding: 5px;
            margin : 5px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: auto;
        }

        .box-content {
            background-color: #f0f0f0;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 5px;
            margin: 3px;
        }

        .box-content ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .box-content ul li {
            margin : 5px;
        }

        #back {
            max-width: 1000px;
            margin: auto;
            padding: 10px;
            text-align: center;
            
        }
    </style>
</head>

<body>
    <div class='container'>
        <h3> Employés ayant reçu plus de {{ nombre_min }} mails entre {{ date_debut }} et {{ date_fin }} </h3>

        {% if plus_de_x_mails_envoyes%} 
            <h4> Nombre de messages envoyés par les employés: </h4>
            {% for employe in plus_de_x_mails_envoyes %}
            <div class="box">
                <div class="box-content">
                    <ul>
                    <li> <strong>{{ employe.0}}</strong> </li>
                    <p> Echanges internes {{ employe.1}} - échanges externes: {{ employe.2}} </p>
                    </ul>
                </div>
            </div>
            {% endfor %}
        {% endif %}
    </div>

    <div class='container'>
        {% if plus_de_x_mails_recus %} 
        <h4> Nombre de messages reçus par les employés: </h4>
        {% for employe in plus_de_x_mails_recus %}
        <div class="box">
            <div class="box-content">
                <ul>
                <li> <strong>{{ employe.0}}</strong> </li>
                <p> Echanges internes {{ employe.1}} - échanges externes: {{ employe.2}}  </p>
                </ul>
            </div>
        </div>
        {% endfor %}
    {% endif %}
    </div>


    <div id='results' style="display: flex; justify-content: center;" >
        <div class="back-button">
                <button> <a href='{% url "countEmail" %}'> Retour à la recherche </a></button>
        </div>
        <div class="back-button">
            <button> <a href='{% url "index" %}'> Retour à la page d'accueil </a></button>
        </div>
    </div>        
</body>
</html>
